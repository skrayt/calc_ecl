# ECL

- 14:02
  データ閲覧画面。
  1.  以後のページ同様データ変換機能が欲しい。
  2.  説明変数と目的変数は同じ画面でプロットが見たい。上部に目的変数、下部に説明変数が並ぶように。新しいタブを用意して。
  3.  新しいタブの中で一つ一つのプロット機能とは別に、選択した変数を一つのグラフの中で表示する機能が欲しい。選択できる変数は目的編説明変数ともに可。
  4.  目的変数データインポートは説明変数と違い、毎年レコードを追加していく運用なので、レコード追加で対応して。ただし、初回に一括インポートする必要があるので、GUI機能として持たせるか、別途一括取込のモジュールが欲しい。
  5.  目的変数タブにあるデータセット名を入力するテキストフィールド、目的変数タイプの選択、単位のフィールドは必要？何か機能している？
  6.  セグメント情報も何か使っている？絞り込み？

  #ECL将来予想モデル

- 14:15
  相関分析ページ。
  1.  説明変数の選択欄がデータ閲覧画面のそれと異なり旧式のまま。
  2.  動的回帰のページと違い、この画面はデータ変換が一括でしか選べないが、これは想定通りの仕様？
  3.  相関分析にかかるプロットや指標の提示は他にないか？
- 14:27
  回帰分析ページ。
  1.  説明変数の選択欄がデータ閲覧画面のそれと異なり旧式のまま。
  2.  動的回帰のページと違い、この画面はデータ変換が一括でしか選べないが、これは想定通りの仕様？
  3.  交差検証の分割数とは？より詳しい悦明が欲しい。
  4.  ラグは一律にしか加えられないのか？ラグの影響のある変数とラグの影響のない変数同士の組み合わせは想定されないか？もしくはそこまですると複雑になる？
  5.  交差検証の結果、残渣プロットに関して解釈に対する説明を充実させてほしい。
  6.  残渣ヒストグラムの説明を時系列残渣プロットという項目で説明している？
  7.  交差検証の結果が平均MSEしか画面には見えないが、説明書にはMSE標準偏差や各foldのMSEに関する記述がある。これらの数値はどこにある？

- 14:38
  モデル選択ページ。
  1.  説明変数の選択欄がデータ閲覧画面のそれと異なり旧式のまま。
  2.  動的回帰のページと違い、この画面はデータ変換が一括でしか選べないが、これは想定通りの仕様？
  3.  モデル候補の評価結果のfeaturesの値と項目名は日本語で表記し、レコードには説明変数ごとに改行して表示してほしい
  4.  評価結果の各項目の中で優位（有意）だった値には色を付けるなどは可能か？
  5.  ラグは一律にしか加えられないのか？ラグの影響のある変数とラグの影響のない変数同士の組み合わせは想定されないか？もしくはそこまですると複雑になる？
  6.  組み合わせ探索実行ボタンの上に出ている分数はすべての組み合わせ数と、表示された評価結果の分数なのだと思うが、「VIF<=10のみ表示」のトグルをオンにしても全数/全数にしかならず、意味がない？

- 15:46
  動的回帰ページ。
  1.  各ページで変数別に変換及びラグを設定できればこのページは不要？変数別設定をオンオフするトグルが各ページにあればよいのでは？
  2.  残したとして、説明変数は変換や標準化ができるのに対し、目的変数は変換も標準化も出来ない。ほかのページも目的変数は変換も標準化もしていない？標準化した変数と標準化していない変数を投入して出た結果に何か意味はある？

- 15:53
  ARIMAページ。
  1.  ADF検定のnlagsデフォルト値を20としているが、説明変数の値が40未満なので、デフォルト値をとることができない。デフォルト値およびmaxlagを指定しなければ適切なラグを自動で指定してくれるらしいので、デフォルト値を指定せず、かつ好きなnlagsを設定できるよう入力フィールドが欲しい。
  2.  本番では説明変数は外部シンクタンクから購入する予定であり、ARIMA分析でもとめた説明変数の予測値を利用する予定はない。しかし開発時には予測値を持っていないので、ARIMAで求めた予測値を使う予定である。従って、将来シナリオの画面では設置した入力フィールドに入力された値を優先採用し、入力フィールドに値が無ければARIM分析で求めた値を採用する方式にしてほしい。
  3.  ARIMAモデルはパラメータや解釈すべき指標が多く、回帰分析等よりも複雑なモデルなので、実務担当者にもっと寄り添った解説やマニュアルを加えてほしい。

- 09:08
  データ閲覧ページ。
  1.  変換のリストは出来ているが、標準化のトグルスイッチがない。
  2.  グラフの大きさがバラバラ。グラフタイトルの長さによってプロットのサイズがずれることが原因と思われる。
  3.  frequencyは包含的な取り扱いをしてほしい。例えば月次は四半期次に内包され、四半期次は年次および年度次に内包されるので、内包されるデータはそれぞれのfrequencyに含めてほしい。含めるデータはそれぞれのfrequencyの日付と一致するレコードのみで可。
  4.  上記3の変更に伴い、frequencyの日付は期初ではなく期末の月を取得してほしい。
      例）月次：1日→末日、四半期次：1月→3月、年次：1月→12月、年度次：4月→翌年3月
  5.  上記1～4は説明変数データ・目的変数データともに同様の対応をしてほしい。
  6.  比較プロットタブの0-1正規化（スケール統一）の表現だが、標準化ではなく正規化が正しいのか？
  7.  2軸プロットのトグルボタンは必要か？説明変数も目的変数も全選択してテストしたが、2軸目のメモリは2つ目以降の変数全部が2軸目になっていて、あまり効果がなさそう。2軸目にする変数名を選択させるほうが良いと思う。
- 10:02
  回帰分析ページ。
  1.  係数テーブルにVIF値が欲しい。相関分析タブで確認してはいるが、タブを切り替えると別タブの情報が見れないので、説明変数が多いとVIF値が確認するためににタブの切り替えが頻発する。DW値で補完可能？
  2.  モデル選択タブとこのタブとの違いは？
- 10:19
  動的回帰ページ。
  1.  目的変数の標準化機能は必要ではないか？説明変数は標準化した0～1の値と、標準化していない0～1の値を比較して、解釈が可能なのか？例えば説明変数の一つである失業率も0～1の間にあるが、これは標準化してもよいのか？implementation_pla.mdのPG/LGDは標準化しないという記述の根拠と矛盾するのではないか。
- 10:22
  ARIMAページ。
  1.  DB保存は出来たが、どの決算年度のために作成したARIMAモデルなのかを指定する必要があるのではないか。ECL計算は同じ決算年度同士のデータを用いるべき。
  2.  同じ決算年度で同じ説明変数のARIMAモデルを作成した時、上書き保存するか確認が必要では？もしくは作成したモデルを削除する機能。試行を重ねるうちにDBに保存されたモデルが増えすぎて、どれが正当なのか分からなくなりそう。
- 10:32
  モデル確定ページ。
  1.  DB保存時に決算年度も併せて記録しておく必要あり。決算年度：目的変数＝説明変数＝予測モデル＝ARIMAモデルが一貫していることは大前提。
  2.  モデルそのものは保存されていない？使用した指標や統計情報はJSONで保存されている。
  3.  ID表示は必要？不要なモデルを削除したらIDの値が飛び飛びになってしまい、故意に削除したのではないかと違和感を感じる。
  4.  DBボタンを複数回クリックすると全く同じモデルでも何回でも保存できてしまう。既存のモデルが存在しないかチェックが必要。既に存在していれば保存不可。
- 10:34
  ECL計算ページ。
  1.Step1のセクション委に「エラー：`Timestamp` object has not sibscriptable」とメッセージ表記がある。
